[Test]
public void When_{{ property_name }}_IsValid_Then_Success()
{
    // Given
    var message = "A valid {{ property_name }} message";
    {{ property_type }} expectedValue = {{ valid_value }};
    var instance = new {{ class_name }}();
    instance.{{ property_name }} = expectedValue;

    // When
    var result = instance.{{ property_name }};

    // Then
    result.Should().Be(expectedValue);
}
